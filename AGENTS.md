# Repository Guidelines

- このリポジトリは Windows 向け画像小分けアプリ PicPack です。
- 読みやすさと安全なファイル操作を最優先にしてください。
- ファイル移動・削除・上書きに関わる変更は慎重に行ってください。
- 既存ファイルを上書きしないでください。
- UI を変更する場合も、処理ロジックは `Services` 配下へ分離してください。
- `dotnet build` と `dotnet test` が通る状態を維持してください。
